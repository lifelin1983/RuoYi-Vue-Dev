# 2026-09-02 工作日志

## 刷新 Harness 合规评估报告（对齐 Level 1 真实状态，2026-09-02 08:00）
- 用户"要"→ 把上一轮落地的 Level 1 监控型 check 写进报告熵管理评分，并一次性把报告对齐到最新真实状态（此前报告严重滞后于代码）。
- 改动 Harness-Engineering-合规评估报告.html：
  - 顶部新增 2026-09-02 更新横幅（Level 1 落地：coverage_delta/new_untested_class 监控型 check，commit ae7c0aa+0d47475）。
  - 熵管理评分 91→94（bar + 说明段）：基线已 nightly 回写根治、手动熵追踪演进为自动巡检、新增两种监控型 check；剩余扣分仅 field-sync 仍 manual_review。
  - 记分卡第三节熵管理：原"手动熵追踪(部分/可演进为自动巡检)"改为"已演进→符合"，并新增"监控型 check"行(符合)。
  - 整改优先级表：原"建议 P2 基线回写自动化"改为 P1✅(commit 8976f9a 等已完成)；新增 Level 1 行 P1✅。
  - 实地核验表新增"Level 1 监控型 check 是否真生效"双向验证行。
  - enforcement.yml 卡片、verdict 段、页脚下一步措辞同步更新（含 Level 2 待定）。
- 评级维持 A；报告未提交 git（作为评估交付物留工作区，是否入仓待用户定）。
- 教训：报告类交付物易滞后于代码落地，应在每次实质改动后同步刷新，而非堆积到"下一步"。

## Level 2 PR 债务护栏落地（2026-09-02 07:50，commit b89d67a + 报告 9fd1667）
- 用户"继续"→ 推进熵管理巡检主线 L0→L1→L2：Level 2 = PR 触发时检测覆盖率退化/新增未测类，自动发 PR 评论(含可一键采纳的 DEBT 草稿)。
- scripts/pr-debt-guard.py（零依赖）：复用 nightly 已回写的 measured_baseline(enforcement.yml, base/main 实测真相) 作 diff 基准；对比 PR head 的 domain 整包 LINE% 与 service.impl 未测类(find_untested_classes 同源 audit 脚本)；命中生成 PR 评论 Markdown(含 DEBT-DRAFT-* 草稿 YAML)，告警非阻断(exit 0)。
- ci.yml 新增 pr-debt-guard job(on: pull_request, needs build-and-test 复用 jacoco-reports artifact, gh pr comment 发评论, permissions: pull-requests: write)。
- 关键设计取舍：不自动写台账文件——fork PR 供应链风险 + GITHUB_TOKEN 无 fork 写权 + 避免与 Gate 2 硬门禁重复阻断；草稿以评论形式贴出待人采纳(粘贴到 harness-debt.yaml 改名即纳入 nightly)。
- 反向测试实锤：impl 丢 TempOrphan.java → 命中 new_untested_class 生成草稿；放假 jacoco.csv(domain 1%) → 命中 coverage_delta 退化草稿；均干净还原。
- 修一误报：head 实算 66.6666… 与 base 声明 66.67 的浮点噪声致 domain 退化恒定误报，加 0.5pp 退化容差(DEGRADE_TOL_PP)消除；无 jacoco 时 coverage_delta 降级 SKIP。
- 报告刷新(9fd1667)：熵管理 94→96，记分卡/整改表/核验表各加 Level 2 行，页脚仅剩 restrictions 待定。
- 教训：对比"声明基线值"与"实算值"必加测量容差，否则浮点噪声会恒定误报(同 DEBT-005 的 0.67pp 余量思路)。

## restrictions 实测不可用 + 维持现状决策（2026-09-02 08:00）
- 用户\"执行\"→ 尝试为 main 加 restrictions 彻底禁直推。实测 GitHub API 返回 422: Only organization repositories can have users and team restrictions —— 个人账号仓库(lifelin1983/RuoYi-Vue-Dev)不支持 branch protection 的 restrictions，无法原生禁直推。
- 经 AskUserQuestion 确认，用户选择\"维持现状\"：不动分支保护，直推 main 保持报告型；PR 合入路径 Gate1/Gate2 阻断已生效不受影响。
- 报告三处措辞刷新(Harness-Engineering-合规评估报告.html)标注此边界与决策。
- 教训/边界：GitHub 的\"限制可 push 人员(restrictions)\"是 Org-only 功能，评估\"禁直推\"方案前必须先确认仓库归属(user vs org)，否则会踩 422。

## 前端测试 P2-4 执行落地（2026-09-02 08:30，commit 见下）
- 用户"前端测试（P2-4）执行"→ 落地此前标"暂缓"的前端测试项。
- 技术栈实测：ruoyi-ui = Vue 2.6.12 + vue-cli 4(webpack)，零测试框架；src/utils 有 23 个纯函数文件，是理想单测对象。
- 关键纠正：harness-debt.yaml 的 DEBT-001 原注"前端不在本仓"是误判——ruoyi-ui/ 就在本仓库！真实边界是 nightly-audit-debt(Python)只扫 Java 测试类，无法校验前端 jest。故 verifiable:false 在"nightly 能否校验"语义下仍成立，但理由须改。
- 落地 MVP（jest 路线）：
  - 增量装 jest@29 + babel-jest@29 + @babel/core@^7 + @babel/preset-env@^7 + jest-environment-jsdom@29(--legacy-peer-deps 绕过 vue-cli4 老包 peer 冲突; 注意 jest-environment-jsdom 必须锁 29 匹配 jest 29, 否则默认拉 30 不兼容)。
  - ruoyi-ui/jest.config.js(testEnvironment jsdom, babel-jest 转译, collectCoverageFrom 仅 validate.js 保持总覆盖率干净)。
  - ruoyi-ui/tests/validate.spec.js: validate.js 12 纯函数 25 用例全绿, 覆盖率 95.83%(未覆盖 line116 为 isArray 防御性分支, 现代环境不可达)。
  - 发现 validate.js isEmpty(0)/isEmpty(false) 因 JS 宽松相等(0==""/false=="")返回 true —— 这是框架真实语义, 测试断言已修正为如实反映并加注释标注陷阱(不改上游代码)。
  - package.json 加 test/test:coverage script; .gitignore 忽略 coverage/(仓库本就忽略 package-lock.json, CI 用 npm install)。
  - ci.yml 新增 frontend-test job(报告型: push/pull_request 触发, Node 22, 不加入 required_status_checks, 不阻断合入; 若未来要硬阻断把 job 名加入分支保护 required_status_checks contexts)。
- 提交: A=代码配置(ci.yml+harness-debt+package.json+jest.config+tests+.gitignore); B=报告刷新; C=本 memory。
- 教训: ① GitHub 个人仓库 restrictions 不可用(前轮) + ② 评估"前端不在本仓"前必须实际 Glob 确认, 不要凭台账注释臆测; ③ jest-environment-jsdom 大版本须与 jest 主版本匹配。
