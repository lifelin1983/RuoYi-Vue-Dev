# 2026-09-02 工作日志

## 刷新 Harness 合规评估报告（对齐 Level 1 真实状态，2026-09-02 08:00）
- 用户"要"→ 把上一轮落地的 Level 1 监控型 check 写进报告熵管理评分，并一次性把报告对齐到最新真实状态（此前报告严重滞后于代码）。
- 改动 Harness-Engineering-合规评估报告.html：
  - 顶部新增 2026-09-02 更新横幅（Level 1 落地：coverage_delta/new_untested_class 监控型 check，commit ae7c0aa+0d47475）。
  - 熵管理评分 91→94（bar + 说明段）：基线已 nightly 回写根治、手动熵追踪演进为自动巡检、新增两种监控型 check；剩余扣分仅 field-sync 仍 manual_review。
  - 记分卡第三节熵管理：原"手动熵追踪(部分/可演进为自动巡检)"改为"已演进→符合"，并新增"监控型 check"行(符合)。
  - 整改优先级表：原"建议 P2 基线回写自动化"改为 P1✅(commit 8976f9a 等已完成)；新增 Level 1 行 P1✅。
  - 实地核验表新增"Level 1 监控型 check 是否真生效"双向验证行。
  - enforcement.yml 卡片、verdict 段、页脚下一步措辞同步更新（含 Level 2 待定）。
- 评级维持 A；报告未提交 git（作为评估交付物留工作区，是否入仓待用户定）。
- 教训：报告类交付物易滞后于代码落地，应在每次实质改动后同步刷新，而非堆积到"下一步"。

## Level 2 PR 债务护栏落地（2026-09-02 07:50，commit b89d67a + 报告 9fd1667）
- 用户"继续"→ 推进熵管理巡检主线 L0→L1→L2：Level 2 = PR 触发时检测覆盖率退化/新增未测类，自动发 PR 评论(含可一键采纳的 DEBT 草稿)。
- scripts/pr-debt-guard.py（零依赖）：复用 nightly 已回写的 measured_baseline(enforcement.yml, base/main 实测真相) 作 diff 基准；对比 PR head 的 domain 整包 LINE% 与 service.impl 未测类(find_untested_classes 同源 audit 脚本)；命中生成 PR 评论 Markdown(含 DEBT-DRAFT-* 草稿 YAML)，告警非阻断(exit 0)。
- ci.yml 新增 pr-debt-guard job(on: pull_request, needs build-and-test 复用 jacoco-reports artifact, gh pr comment 发评论, permissions: pull-requests: write)。
- 关键设计取舍：不自动写台账文件——fork PR 供应链风险 + GITHUB_TOKEN 无 fork 写权 + 避免与 Gate 2 硬门禁重复阻断；草稿以评论形式贴出待人采纳(粘贴到 harness-debt.yaml 改名即纳入 nightly)。
- 反向测试实锤：impl 丢 TempOrphan.java → 命中 new_untested_class 生成草稿；放假 jacoco.csv(domain 1%) → 命中 coverage_delta 退化草稿；均干净还原。
- 修一误报：head 实算 66.6666… 与 base 声明 66.67 的浮点噪声致 domain 退化恒定误报，加 0.5pp 退化容差(DEGRADE_TOL_PP)消除；无 jacoco 时 coverage_delta 降级 SKIP。
- 报告刷新(9fd1667)：熵管理 94→96，记分卡/整改表/核验表各加 Level 2 行，页脚仅剩 restrictions 待定。
- 教训：对比"声明基线值"与"实算值"必加测量容差，否则浮点噪声会恒定误报(同 DEBT-005 的 0.67pp 余量思路)。
