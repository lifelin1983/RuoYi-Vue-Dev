# 2026-08-31

## Harness Engineering 合规评估（.claude + docs）
- 任务：以 Harness Engineering（驾驭工程，AI 工程化范式，Agent=Model+Harness）为基准，评估仓库 `.claude/CLAUDE.md`、`docs/`（含 `.harness/enforcement.yml`）的合规度。
- 结论：B+。上下文工程 + 架构约束两支柱扎实（规则有事故溯源、ArchUnit 15 条可机器执行、@Disabled 固化缺陷=棘轮实践）；短板在"外环自动化"——CI 闸门未接（`.github/workflows/` 不存在）、无 pre-commit Hook、缺熵管理/文档一致性巡检。
- 实地核验：ArchUnit 测试类真实存在；2 个 @Disabled 缺陷用例在位；api-spec.yaml 669 行非空壳；CLAUDE.md↔docs 互链人工可解析但未自动化。
- 交付物：`Harness-Engineering-合规评估报告.html`（仓库根目录，单文件内联、含评分条形图与整改优先级 P0/P1/P2）。
- 用户偏好确认：把"Harness 工程驾驶原则"明确为"Harness Engineering 驾驭工程"范式。

## P0-P2 详解（补充核验，含新发现）
- **注意：仓库里有两套 P0/P1/P2 编号**，讲解时必须区分：
  1. `docs/plans/current-sprint.md` 的业务任务编号（P0-1..P0-7 / P1-1..P1-6 / P2-1..P2-5）
  2. `.harness/enforcement.yml` 的 `ci_jobs` 优先级 + 评估报告的"整改优先级"
- **新发现的文档漂移（活标本）**：`.claude/CLAUDE.md` §已知欠账 仍写着"全仓库**零测试**，`mvn test` 目前不跑任何用例"，实际已是 56 用例全绿 → 这是 P0「knowledge-freshness」必要性的最强实证。
- 实地核验补充：`.github/` 存在但只有 `FUNDING.yml`，无 `workflows/`；`.git/hooks` 全部是 sample，无生效钩子；`.claude/` 下无 `settings.json`（无 Hook 配置）；根 POM 与 ruoyi-admin POM 均无 jacoco；`ruoyi-ui/` 无 `tests/` 目录、package.json 无 test 脚本、devDeps 无 jest（P2-4 需先装 `@vue/cli-plugin-unit-jest`）。
- CI 关键约束：Controller 鉴权测试依赖 Redis（`application.yml` redis database: 0，本地建议 database: 5 隔离），CI 必须起 redis service；`ruoyi-admin` 目前无 `application-test.yml`。
- Maven 命令坑（已记入 enforcement.yml）：`-pl ruoyi-admin` 必须带 `-am`，否则兄弟模块被当外部依赖报 Could not find artifact。

## P0-2 落地：文档新鲜度校验器（已交付，只报未改）
- 交付物：`scripts/check-doc-links.sh`（bash 4+，`--strict` 让 warning 也失败）。6 类检查：CLAUDE.md 引用路径 / enforcement.yml 的 docs+test_classes / 全部 md 相对链接 / total_tests 一致性 / pending_defects 方法存在性 / "零测试"过期声明矛盾检测。
- **实测结果**：真实仓库 36 项检查通过、**0 断链**、4 条 warning；反向测试（临时 fixture）精确抓到 2 处并 exit 1。
- **发现的 4 处过期表述**（待老大确认后再改）：
  - `.claude/CLAUDE.md:90`「已知欠账：全仓库零测试」（最危险，Agent 入口文档在说谎）
  - `docs/conventions/testing.md:3`「现状：本仓库当前零测试」
  - `docs/architecture/overview.md:301`「待改进：全仓库零自动化测试」
  - `docs/conventions/testing.md:817`「无 src/test 目录」
  - ✅ `docs/plans/current-sprint.md:17` 属**正确的历史基线**，已写入脚本 `STALE_ALLOWLIST` 豁免（保留审计痕迹）。
- **校验器自身的三个坑（务必记住）**：
  1. ArchUnit 规则是 `@ArchTest` **字段**不是 `@Test` 方法——只数 `@Test` 得 45，少 11，会伪装成"测试数漂移"假阳性。必须 `grep -cE '^[[:space:]]*@(Test|ArchTest)'`。
  2. CLAUDE.md 里的链接多为**仓库根相对**（`docs/x.md`），按文件相对解析成 `.claude/docs/x.md` → 误报。必须双路径回退（先文件相对、再根相对，两者都缺才判失败）。
  3. 同一断链会被"路径扫描"和"链接扫描"两个 checker 各报一次 → 用 `declare -A` 按路径 key 去重。
- 教训印证：校验器本身也必须做反向测试，否则它"静默通过"时你分不清是真的干净还是根本没提取到东西（本次 check 2 就差点这样蒙混过关）。

## 文档修正（老大批准后执行，已完成）
- 改动文件 5 处：
  1. `.claude/CLAUDE.md` 已知欠账：删「全仓库零测试」，改为「Controller/前端测试未覆盖（P1-5/P2-4）」+「CI 门禁与 JaCoCo 未接入，约束仅本地生效」
  2. `docs/conventions/testing.md:3` 改为「已有 56 用例全绿，2 个 skipped 是 @Disabled 固化缺陷」
  3. `docs/conventions/testing.md:817` 改为「`src/test` 已建立…」
  4. `docs/architecture/overview.md` 待改进表：删「全仓库零自动化测试」，补「CI 质量门禁未接入」「Controller/前端测试缺失」
  5. `docs/plans/current-sprint.md` §8 变更记录新增 2026-08-31 条目
- **连带修复（重要）**：`.harness/enforcement.yml` 里 `knowledge-freshness` 仍标 `status: pending` —— 门禁已落地却说没落地，**这本身是新漂移**。已改 `active` + 补 `command` + `negative_test`。同时把 `architecture-boundary-check` 的 `status: active` 改为 `command_verified` + 新增 `wired_into_ci: false`（原措辞有歧义，曾误导首版评估报告）。
- **校验器第 4 个坑**：写完变更记录后，条目里「修正"零测试"过期表述」这句话本身含"零测试"，被自己抓成假阳性。
  → 改为**按章节豁免**而非按行号豁免：awk 追踪标题层级，从 `变更记录|变更历史|迭代背景` 起豁免，遇到同级或更高级标题即结束。行号豁免一改文件就失效。
  → 已做边界反向测试：豁免章节前、后各埋一处真声明，均被正确捕获，豁免不会外溢。
- **最终状态**：`bash scripts/check-doc-links.sh --strict` → 36 项通过 / 0 error / 0 warning / RESULT: PASS / exit 0。反向测试 fixture 复测：2 error + 2 warning，exit 1。
- Skill `harness-doc-freshness-gate` 已同步更新（新增章节豁免与"落地后回查约束文件"两条经验）。

## P0-1 落地：接入 CI 闸门（已完成配置 + 命令实跑验证）
- 交付物：`.github/workflows/ci.yml`（双并行闸门）：
  - Gate 1 `docs-freshness`：跑 `bash scripts/check-doc-links.sh --strict`（秒级、不需 JVM）。
  - Gate 2 `build-and-test`：JDK8（temurin）+ Redis service（redis:6-alpine，Controller 鉴权测试需）+ 两步 `mvn`：`2a` 架构约束（`-pl ruoyi-admin -am -Dtest=ArchitectureRulesTest,MapperXmlRulesTest -DfailIfNoTests=false`）、`2b` 全量 `clean test`。含 surefire 报告上传。
- **三条 mvn 命令全部本地实测通过**（Windows 用 `mvn.cmd`，Git Bash 下裸 `mvn` 报"找不到主类 Launcher"）：
  - 架构命令：15 用例（1 @Test + 11 @ArchTest + 3 @Test）BUILD SUCCESS
  - 业务命令 `mvn test -pl ruoyi-system -am -Dtest='Sys*Test' -DfailIfNoTests=false`：41 用例（Service 18 + Mapper 23），2 skipped，BUILD SUCCESS
  - 全量 `clean test`：56 用例，2 skipped，BUILD SUCCESS
- **踩坑（实跑才暴露，文档原命令是坏的）**：CLAUDE.md 与 enforcement.yml 里 `-am -Dtest` 命令**缺 `-DfailIfNoTests=false`**，会导致 ruoyi-common 等无匹配模块报 `No tests were executed!` 直接失败。已给架构、业务命令和 enforcement.yml `command` 字段全部补齐。
- **连带修正 enforcement.yml**：`architecture-boundary-check` 与 `knowledge-freshness` 的 `wired_into_ci` 翻 `true`、`status` 改 `active`；删除 `still_missing` 里的「CI 流水线」；`enforcement.command`（行 32）也补 `-am -DfailIfNoTests=false`。
- **重要前提（必须告知老大）**：本仓库**当前不是 git 仓库**（无 `.git`/remote），`.github/workflows/ci.yml` 需推送到 GitHub 并启用 Actions 后才会真正拦截合入。其余 P1（pre-commit Hook、JaCoCo）、P2（Controller/前端测试）尚未做。
- 评估报告同步：顶部横幅加 P0-1 已落地 + 前提警告；记分卡 ②架构约束 82→90、质量门禁 28→55（amber）。freshness 复跑仍 36/0/0 PASS。
- Skill `harness-doc-freshness-gate` 补第 7 条坑：Maven reactor + `-Dtest` 必加 `-DfailIfNoTests=false`，且 `-am` 不可省；Windows 用 `mvn.cmd` 实跑验证命令后再写 CI。

## 连入 GitHub（Phase 0，老大提出"我还想连入github"）
- **关键阻断点已修**：原 `.gitignore` 缺 `node_modules/`（且 `ruoyi-ui/node_modules` 1.8万+文件存在），直接 push 会传上 G 依赖。已补 `node_modules/`、`ruoyi-ui/dist/`、`dist/`、`logs/`、`.DS_Store`、`Thumbs.db`；`ruoyi-ui/.gitignore` 本身只忽略 node_modules/dist/lock，未动 src，前端源码完整。
- **已执行**：`git init -b main`（默认分支设为 main，对齐 GitHub 现行约定 + ci.yml 的 `on.push.branches:[master,main]`）。`git check-ignore` 确认 node_modules 被忽略；`git add -n` 待提交 **682 个文件**（ruoyi-ui/src 293 个，仓库完整无泄漏）。
- **环境限制**：本机有 git 2.55，但**无 gh CLI** → GitHub 建空仓库需老大在网页建（或我先装 gh），我再加 remote + push。
- **对 JaCoCo 计划（P1）影响**：技术步骤零变化（plugin→实测覆盖率→定门槛→ci.yml Gate 2b 改 verify）。变的是 CI 终于"通电"。
- **建议时序**：先连 GitHub 推一版验证已有两道门禁在 Actions 真绿 → 再上 JaCoCo 推第二版（避免门槛定高导致首版即红）。
- 当前状态：本地 git 已就绪（见下方『连入 GitHub 收尾』——已 commit+push+CI 通电）。

## 连入 GitHub 收尾（已完成）
- **装 gh**：winget 装 GitHub CLI v2.98.0（路径 `C:\Program Files\GitHub CLI\gh.exe`）。本机无 `$HOME/.config/gh` 文件，凭据走 Windows keyring。
- **登录**：老大网页登录 GitHub 后，本机终端 `gh auth login` 成功 → 账号 `lifelin1983`，token scopes 含 `repo` + `workflow`（够建仓库+跑 Actions）。
- **初始提交 + 建仓库**：commit `2b329d0`（682 文件，含 `.workbuddy` 项目记忆）；`gh repo create RuoYi-Vue-Dev --private --source=. --push` 建**私有**仓库并推 `main`。
  - ⚠ 坑：`gh repo create` **不支持 `--branch` 参数**，加上会整条命令参数解析失败、只打印帮助、仓库未建。去掉 `--branch` 后即成功（本地已 `git init -b main`）。
  - 仓库 URL：https://github.com/lifelin1983/RuoYi-Vue-Dev
  - git 身份（repo 级）：`user.name=lifelin1983`，`user.email=lifelin1983@users.noreply.github.com`
- **CI 已通电验证**：Actions run `#33380657399` 实测 `success`。
  - Gate 1 knowledge freshness（Check documentation drift）→ ✅
  - Gate 2 architecture and tests → 2a architecture boundaries ✅ / 2b full test suite ✅
  - **三道门禁在 GitHub 全部变绿**，约束从「本地强制」升为「不可合入门禁」。
- 评估报告同步：P0-1 横幅「前提警告」改为「✅ 已通电验证」并附 run 链接；质量门禁 28→55 维持（pre-commit Hook 与 JaCoCo 仍未接）。

## JaCoCo 覆盖率门禁落地（P1，本日续做）
- **实测基线**（ruoyi-system/target/site/jacoco/jacoco.csv）：`com.ruoyi.biz.service.impl` 100%（SysProduct/SysStudent），`com.ruoyi.biz.domain` ~50%，`com.ruoyi.biz` 整包 ~70%。
- **发现**：老大的 `com.ruoyi.biz` 包已有真实单测（SysProductServiceImplTest/SysStudentServiceImplTest + Mapper 测试），RuoYi 核心(service.impl/system)与 common.utils 仍 0%。
- **pom.xml**：注入 `jacoco-maven-plugin`（prepare-agent+report+check），全模块继承；check 三条 rule 仅 scope `com.ruoyi.biz`，`haltOnFailure=true`。
- **ci.yml**：Gate 2b `clean test`→`clean verify`，新增 `jacoco-reports` artifact 上传。
- **反向测试实跑**：临时塞 `TempProbeServiceImpl`（0% 覆盖）→ `clean verify` BUILD FAILURE（jacoco:check rule violated 0.00<1.00）；删除后恢复 SUCCESS。证明门禁非假绿。
- **连带**：enforcement.yml `coverage-gate`→active/wired_into_ci:true + 实测基线；CLAUDE.md 验证命令改 `clean verify` 并补覆盖率说明；新建 docs/CHANGELOG.md；报告 HTML 质量门禁 55→78、P1 JaCoCo 行转「已落地」。
- **待办**：pre-commit Hook、dependency-direction 约束扫描、熵管理周期巡检（均增强项）；RuoYi 核心/common.utils 单测补齐后扩大 check 的 includes。
